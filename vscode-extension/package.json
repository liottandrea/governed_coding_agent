{
  "name": "governed-coding-agent",
  "displayName": "Governed Coding Agent",
  "description": "AI coding assistant powered by the Governed Coding Agent — terminal-native, HITL-aware, multi-model",
  "version": "0.2.0",
  "publisher": "governed-coding-agent",
  "license": "MIT",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": ["AI", "Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {

    "commands": [
      {
        "command": "governed-coding-agent.openTerminal",
        "title": "Governed Coding Agent: Open Terminal",
        "icon": "$(terminal)"
      },
      {
        "command": "governed-coding-agent.newSession",
        "title": "Governed Coding Agent: New Session",
        "icon": "$(add)"
      },
      {
        "command": "governed-coding-agent.refreshSessions",
        "title": "Governed Coding Agent: Refresh Sessions",
        "icon": "$(refresh)"
      }
    ],

    "viewsContainers": {
      "activitybar": [
        {
          "id": "governed-coding-agent-sidebar",
          "title": "Governed Coding Agent",
          "icon": "resources/icon.svg"
        }
      ]
    },

    "views": {
      "governed-coding-agent-sidebar": [
        {
          "id": "governed-coding-agent.chatView",
          "name": "Chat",
          "type": "webview"
        }
      ]
    },

    "menus": {
      "view/title": [
        {
          "command": "governed-coding-agent.newSession",
          "when": "view == governed-coding-agent.chatView",
          "group": "navigation@1"
        },
        {
          "command": "governed-coding-agent.openTerminal",
          "when": "view == governed-coding-agent.chatView",
          "group": "navigation@2"
        }
      ]
    },

    "configuration": {
      "title": "Governed Coding Agent",
      "properties": {
        "governedCodingAgent.binaryPath": {
          "type": "string",
          "default": "governed-coding-agent",
          "description": "Path to the governed-coding-agent binary (defaults to PATH lookup)"
        },
        "governedCodingAgent.agentHome": {
          "type": "string",
          "default": "",
          "description": "GOVERNED_AGENT_HOME directory (leave blank to auto-detect)"
        },
        "governedCodingAgent.dataClass": {
          "type": "string",
          "enum": ["public", "internal", "restricted"],
          "enumDescriptions": [
            "Data may be sent to external frontier models",
            "Data stays within approved enterprise APIs",
            "Strictly local models only — no external calls"
          ],
          "default": "internal",
          "description": "Data classification — controls which model groups are permitted"
        },
        "governedCodingAgent.role": {
          "type": "string",
          "enum": ["planner", "codegen"],
          "default": "planner",
          "description": "Primary agent role"
        },
        "governedCodingAgent.plannerModel": {
          "type": "string",
          "enum": ["cheap", "mid", "frontier", "local_fast", "local_standard", "local_heavy"],
          "enumDescriptions": [
            "Haiku — fastest, lowest cost",
            "Sonnet — balanced",
            "Sonnet 4.6 — highest quality",
            "phi4-mini — local, fast",
            "devstral — local, balanced",
            "qwen 35B — local, best quality"
          ],
          "default": "mid",
          "description": "Model group for the planner role"
        },
        "governedCodingAgent.subagentModel": {
          "type": "string",
          "enum": ["cheap", "mid", "frontier", "local_fast", "local_standard", "local_heavy"],
          "default": "cheap",
          "description": "Model group for sub-agent roles (codegen, knowledge, summary, testing)"
        },
        "governedCodingAgent.includeKnowledge": {
          "type": "boolean",
          "default": true,
          "description": "Include knowledge retrieval tool in each session"
        }
      }
    }
  },

  "scripts": {
    "compile": "tsc -p tsconfig.json",
    "watch":   "tsc -watch -p tsconfig.json",
    "package": "vsce package --no-dependencies --allow-missing-repository --skip-license"
  },

  "devDependencies": {
    "@types/node":   "^20.19.43",
    "@types/vscode": "^1.100.0",
    "typescript":    "^5.9.3"
  }
}
